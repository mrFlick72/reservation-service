r2dbc:
  datasource:
    host: localhost
    database: reservation
    username: root
    password: root

logging:
  level:
    root: debug

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: ALWAYS