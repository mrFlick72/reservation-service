version: "2"

services:
  postgres:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: reservation
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root